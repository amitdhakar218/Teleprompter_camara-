import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amitdhakar.teleprompter',
  appName: 'Teleprompter Camera',
  webDir: 'dist'
};

export default config;
